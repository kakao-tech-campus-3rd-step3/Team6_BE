server:
  port: ${SERVER_PORT:8080}

spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:h2:mem:testdb?MODE=MYSQL}
    driver-class-name: ${DB_DRIVER:org.h2.Driver}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: ${DB_PLATFORM:org.hibernate.dialect.H2Dialect}
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

springdoc:
  cache:
    disabled: true
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  swagger-ui:
    enabled: true
    path: /swagger
    tags-sorter: alpha
    operations-sorter: alpha

gemini:
  api-key: ${GEMINI_API_KEY:AIzaSyCEe8eG12SYYmsKGEM54I1TfqQWfugdn68}
  model: ${GEMINI_MODEL:gemini-pro-1.5}

jwt:
  secret-key: ${JWT_SECRET_KEY:NyIfV0Hd4XgDdNpRFdoxH/80ePM9LVN5851R2zfXp/EbrB/HYk0vaZikBK+pJs3mnA4fzWR4kYAdxHhXEgHUfQ==}